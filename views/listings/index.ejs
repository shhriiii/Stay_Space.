<% layout("/layouts/boilerplate") %>

<body>
    <!-- <h3>all listing</h3> -->
    <!-- <form method="GET" action="/listings/new">
        <button>Create new listing</button>
    </form> -->
    <!-- <style>
      #filters {
        
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1.5rem; /* cleaner spacing than margin-right */
        padding: 0.5rem 1rem; /* reduced vertical padding */
        background-color: #1a1a2e;
        border-radius: 1rem;
        box-shadow: 0 0 10px rgba(127, 90, 240, 0.2);
        max-width: 100%;
      }
    
      .filter {
        
        text-align: center;
        opacity: 0.6;
        padding: 0.5rem 0.8rem;
        border-radius: 12px;
        background: linear-gradient(145deg, #1f1f2e, #151522);
        box-shadow: 6px 6px 12px #0c0c14, -6px -6px 12px #26263e;
        transition: all 0.3s ease;
        color: #e0e0e0;
      }
    
      .filter:hover {
        opacity: 1;
        cursor: pointer;
        transform: scale(1.05);
        box-shadow: 0 0 15px rgba(127, 90, 240, 0.5);
      }
    
      .filter p {
        font-size: 0.8rem;
        margin-top: 0.3rem;
        color: #b8b8ff;
      }
    </style>
    
    
    <div id="filters">
      <div class="filter">
        <i class="fa-solid fa-fire"></i>
        <p>Trending</p>
      </div>
      <div class="filter">
        <i class="fa-solid fa-bed"></i>
        <p>Rooms</p>
      </div>
      <div class="filter">
        <i class="fa-solid fa-umbrella-beach"></i>
        <p>Beach Escapes</p>
      </div>
      <div class="filter">
        <i class="fa-solid fa-mountain-sun"></i>
        <p>Mountain Views</p>
      </div>
      <div class="filter">
        <i class="fa-solid fa-city"></i>
        <p>City Lights</p>
      </div>
      <div class="filter">
        <i class="fa-solid fa-paw"></i>
        <p>Pet Friendly</p>
      </div>
      <div class="filter">
        <i class="fa-solid fa-gem"></i>
        <p>Hidden Gems</p>
      </div>
    </div> -->
    
    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3">
    <% for(let listing of allListings){ %>
        <a href="/listings/<%= listing._id %>"  class="listing-link">
    <div class="card col listing-card" >
        <img src="<%= listing.image.url %>" class="card-img-top" alt="listing_img" style="height: 20rem"/>
        <div class="card-img-overlay"></div>
        <div class="card-body">
          <p class="card-text"> <b><%=listing.title %> </b>
          <br>
          &#8377; <%= listing.price.toLocaleString("en-IN")  %> / night
         </p>
        </div>
      </div>
    </a>
      <% } %>
    </div>
    
    <!-- <% for(let listing of allListings){ %>
        <li><a href="/listings/<%= listing._id%>"> <%=listing.title %> </a></li>
    <% } %>
    </ul>
</body> -->
